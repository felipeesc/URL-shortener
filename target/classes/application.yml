spring:
  cassandra:
    keyspace-name: url_shortener
    contact-points: localhost
    port: 9042
    local-datacenter: datacenter1
    schema-action: CREATE_IF_NOT_EXISTS

server:
  port: 8080

app:
  base-url: http://localhost:8080
  hashids:
    salt: "url-shortener-secret-salt"
    min-length: 6
